# 更新日志

本项目的版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。
每个版本都会在 [Releases](https://github.com/906351854/DisplayMaster/releases) 页面附带可直接下载的安装包。

## 1.5.0

**后台项不再是「身份不明的开发者」**：登录项交给系统官方的 `SMAppService` 登记，系统设置里从「项目来自身份不明的开发者。」变成 app 自己的图标 +「1 个项目」。代价是需要 macOS 13（Ventura）或更高。

### ✨ 后台项归属到 app 名下

以前是自己往 `~/Library/LaunchAgents/` 写一份 plist 再让 launchd 加载。它能干活，但在系统看来这是一个**没有归属**的裸后台项 —— 「系统设置 → 通用 → 登录项与扩展」里那条记录长这样：

- 名字旁边没有 app 图标，是一张空白的可执行文件占位图；
- 副标题写着「**项目来自身份不明的开发者。**」

现在 plist 作为 app bundle 的一部分（`Contents/Library/LaunchAgents/`）交给系统的 `SMAppService` 登记，同一份东西在面板里变成 app 自己的图标 +「**1 个项目**」。顺带的好处：卸载 app 时系统会一并收拾掉这条后台项，不再留下清理不掉的「幽灵登录项」；升级时也会自动停掉并删除旧 plist 与旧服务，不需要手动做任何事。

行为一个字没变：登录时自动启动、正常退出不复活、崩溃约 10 秒内被重新拉起。

> 那句「身份不明的开发者」是系统对「不属于任何 app 的后台进程」的判语，**和代码签名无关** —— 换注册方式就能去掉，不必非得买 Apple 开发者账号。

### ⬆️ 系统要求提到 macOS 13（Ventura）

`SMAppService` 是 macOS 13 起才有的接口。1.4.2 时把要求降到 12 是为了照顾 Intel 老机型，这次为了上面的改动提到 13：受影响的只有 **macOS 12（Monterey）**；2017 款 MacBook Pro 最高能升到 13 Ventura，仍然可以安装。

## 1.4.4

**撤回救援守护，修掉它的耗电代价**：改成退出时就把内屏还给用户 —— 需要被救援的那个状态本来就不该出现。1.4.3 为保证「拔线必亮」引入的常驻守护，事后看两处代价都不该付：后台空转的耗电，和用户目录里凭空多出一个可执行文件的观感。这一版把它整个删掉：保证一个字没变，机器更干净。

### 🐛 挂着不动也在耗电：救援循环空转

活动监视器里「12 小时电源」743 —— 正常菜单栏 app 是个位数。查下来两个进程各累计了 220 分钟 CPU（运行 54 小时）。

根因是救援的**无界重试**：内屏真的开不回来时循环不会停（这正是当初「拔线必须亮屏」的设计意图），日志里 944 行是同一条失败，横跨 92 分钟。而单次失败并不便宜 —— 每次都要等 2.5 秒确认结果，这 2.5 秒里进程每 80ms 被唤醒一次；**能耗按唤醒次数算，不按 CPU 时间算**，所以它一直进不了空闲。

修复没有削弱保证，只是让「重复做同一件没用的事」停下来：

- **输入指纹闸门**：指纹 = 在线显示器集合 + 睡眠态 + 合盖态。任何真变化（拔线、插线、开合盖、唤醒）立刻放行，一条都不漏；指纹没变而上次失败，就退避 15/30/60/300 秒。自己失败的尝试改不动指纹，所以不会自激。
- **轮询步长 80ms → 250ms**：同样的等待预算，唤醒次数 31 → 10。
- **热路径不再枚举显示器模式**：模式列表是全量扫描里唯一昂贵的一步（一台外接屏 220 个模式），而亮度读写、自动亮度巡检这些每秒都在跑的路径根本不需要它 —— 改前开着自动亮度，等于每秒做一次全量枚举。

实测：稳态 CPU 0.02%~0.03%，**日志 150 秒零增长**（改前常态每 10 秒一行）。

### ♻️ 删掉救援守护：退出时就把内屏还给你

1.4.3 的思路是「出事了再去救」。可要救的那个状态 —— 「app 已经不在、内屏却还关着」—— 本来就不该出现。这一版不让它出现：

- **退出就归还**：菜单退出、`⌘Q`、活动监视器里的「退出」都会先把内屏点亮、清掉记录。app 的运行期偏好一退出就无人执行，留着关掉的内屏就是下一次拔线时的黑屏。
- **活动监视器点「退出」也算正常退出**：AppKit 默认忽略 SIGTERM，以前那样退出等于直接杀进程、内屏留在黑着 —— 现在接进了正常退出流程（5 秒硬超时兜底）。
- **崩溃仍然有人管**：崩溃走不到退出流程，所以保留 launchd 的 GUI 保活 —— 但它只是一条几百字节的规则、不是常驻进程，app 意外挂掉约 10 秒内被重新拉起。
- **老版本升级自动清理**：守护的 launchd 服务、plist、以及 `~/Library/Application Support/DisplayMaster/` 下那个二进制副本，第一次启动时自动删除，不需要手动做任何事。

净效果：后台项目 2 → 1 个，用户目录下不再有可执行文件，代码净减约 30 行。

## 1.4.3

**内屏救援的硬保证 + 外接屏环境光自动亮度**：笔记本用户只要开盖、非睡眠，拔掉显示线内屏一定亮回来 —— 哪怕 app 已经崩溃甚至被手动退出；外接屏还能跟着笔记本的环境光传感器自动调亮度。

### 🛟 双层保活：app 崩了，救援还在

以前「拔线后内屏黑着」的救援逻辑跑在 app 本体里 —— app 一崩溃或被手动退出，救援能力就一起没了。现在分成两层：

- **GUI 保活**（launchd LaunchAgent，SuccessfulExit）：app 意外崩溃约 4 秒内自动拉起；
- **救援守护**（launchd 常驻守护，无条件保活）：一个只干一件事的无界面进程，监听显示器重配置 + 10 秒巡检，发现「外接屏没了、内屏不在线、开盖、非睡眠」就立刻把内屏点亮，并负责把 GUI 服务重新注册回 launchd。

手动退出 app 不会拉起新实例（尊重用户选择），但守护照常在线 —— 救援不再依赖菜单栏那个图标活着。

### 🐛 退出 app 后双击打不开、菜单栏没有图标

救援守护上线后暴露：守护直接跑 `.app` 包内的可执行文件，路径被常驻占用，
LaunchServices 就认为「app 已经在运行」—— 双击弹「已不能再打开」，菜单栏
什么都没有（跑着的是无界面实例）。守护改跑 `.app` 之外的二进制副本
（`~/Library/Application Support/DisplayMaster/`），GUI 每次启动原子刷新
副本，升级后守护自动用上新版本，旧路径注册自动迁移。

### 🐛 守护活着，救援却静默失效

上一条修复的后续：裸二进制副本没有 bundle id，`UserDefaults.standard` 落进
进程名域，GUI 写的全部救援状态（自动开关、内屏关闭记录、内屏 id）守护一个
都读不到 —— 巡检在跑，判定永远是「无需救援」，日志里什么都没有。两份进程
现在显式共享同一个偏好域，读写一份状态。实测：`--auto-test --fake-no-external`
从「四个全空」变为「开关已打开 / 关闭记录 / 内屏记忆 / 判定=打开内屏」。

### ✨ 外接屏跟随环境光自动调节亮度

外接显示器也能借助笔记本的环境光传感器自动调亮度了。macOS 27 已把原始
传感器数据对第三方全部锁死（HID 事件系统 / SMC / IORegistry 实测三条路全封），
这里镜像系统自己的环境光补偿输出：读内置屏的线性亮度作为环境光代理，映射成
外接屏目标亮度（12% 下限、2% 死区防抖），走现成的 DDC 节流写入，**多台外接屏
同时调节**；手动拖滑块后 6 秒内自动亮度让位；读不到环境光（内屏离线/合盖/台式机）
时降级为保持当前亮度。菜单新增开关，默认关闭。

### ✨ 两个自动功能互斥，滑块跟手

「环境光自动亮度」和「有外接屏时自动关闭内置屏」互为反面（环境光读数来自
内屏，关了内屏前者就失效）—— 现在程序内强制互斥：开一个自动关掉另一个，
启动时也会收敛。另外菜单开着时，亮度滑块每秒对齐硬件真值：系统自动调节、
显示器物理按键、别的进程改了亮度，滑块都会跟上来。

## 1.4.2

**两个滑块和显示器「记性」的修复**：分辨率滑块拖了没反应、外接屏重新插上后分辨率被重置回默认档。

### 🐛 分辨率滑块能拖、松手却什么都不发生

滑块「松手才切模式」（边拖边切是频闪）押在了 `mouseUp` 回调上，但 NSSlider 的 cell
在拖动时会自建事件循环、把 mouseUp 自己消费掉 —— 视图的 `mouseUp(with:)` 一次都
收不到（试验台实测 0 次）。于是松手回调从未触发，模式从头到尾没切过：滑块能拖、
重开菜单又弹回去。亮度滑块没暴露这个坑，是因为它靠拖动中的连续 action 就能生效。

修法：松手信号改从动作事件的事件类型里认 —— 松手那一下 action 会带着
`.leftMouseUp` 来。顺带补上分辨率滑块的 `isContinuous`（拖动中的文字预览以前也是死的）。

### ✨ 记住每台显示器的分辨率，重新插上自动恢复

把外接屏切到非默认档，拔线重插，macOS 会按它自己的默认档把屏点起来 ——
上一回的选择丢了。现在每台屏按 **EDID 三要素**（vendor / model / serial，插拔
不会变）记账：在线期间档位变了就更新记忆；重新上线且落点 ≠ 记忆时，约 2 秒内
自动切回。app 启动那一刻不动任何人的分辨率（开机换显示器环境是常态）。

真机实测抓到并堵住的两个隐蔽坑：

- **重连风暴里模式读数会跳变**：刚上线的屏第一次读到的档位是随机的，零点几秒
  后才落定 —— 立刻判定会漏恢复。新屏先挂起，等模式落定再判。
- **刚恢复的档位会被弹回**：插拔引发的重配置会把系统默认配置再套一遍，弹回的
  那一下长得跟「用户改档」一模一样。恢复现场会被记下，6 秒内被弹回落点档的
  判为「系统没站稳」，补切一次；补两次还弹才认输照记。

### ✨ 系统要求从 macOS 14 降到 12：Intel 老机型也能用了

有用户反馈：MacBook Pro 2017（Intel）上 BetterDisplay 能手动关内屏，但老机器
装不了它的新版本，插外接屏自动关内屏的自动化就没有了。本应用的全部功能
（自动关内屏、救援、亮度、分辨率记忆）在这类机型上照常工作。

排查结论：代码里真正用到的系统 API 没有一处超过 macOS 12 —— 之前标的 14
只是清单里的一行声明。把部署目标降到 12 后编译器逐个文件把关通过（唯一一处
14 专属调用已换成老系统可用的等价写法）。2017 款 MacBook Pro 最高能升级到
macOS 13 Ventura，Intel 机型全部覆盖；发布包本来就是通用二进制（x86_64 + arm64）。

### 🔍 诊断

- `--auto-test` 增加「记住的分辨率」一行（每台屏当前记住的档）。
- 场景自测 38 → 45 条（分辨率记忆的判定核心抽成纯函数逐条覆盖）。

## 1.4.1

**修一个会让内屏一直黑着的 bug**：外接屏接着、内屏已被自动关掉，这时合盖 / 睡眠再拔线，内屏不会自己亮回来 —— 面前一块能看的屏都没有，而且怎么等都等不回来。

### 🐛 拔线后内屏不亮：一块「随航残影」被当成了外接屏

日志把现场记下来了（这也是当初加运行日志的意义）：

```
[04:42:32] 配置变化：在线 [无] · 已关闭 1 台 · 睡眠 否 · 另排除虚拟屏 34
[04:43:00] 配置变化：在线 [ (AirPlay)] · 已关闭 1 台 · 睡眠 否
[07:13:54] 配置变化：在线 [ (AirPlay)] · 已关闭 1 台 · 睡眠 否
```

拔线那一刻在线列表里只剩一条**名字叫「 (AirPlay)」的东西** —— 注意前面那个空格，
**设备名是空的**。这是 iPad 随航断开之后留在系统里的一条残影：它 `CGDisplayIsBuiltin`
返回 0，于是被算成「外接屏还接着」，规则判定成「外接屏已接入，内屏本来就没开」，
从那以后一直按兵不动，用户整整黑屏两个半小时。

修法是给这种「用户根本看不见」的条目正名，统一判为**占位屏**，和虚拟屏一样
**不进菜单、也不计入外接屏**（判据：名字属于随航 / 隔空播放这一家子的、
读不到任何 EDID 的、或者有 EDID 却报不出可用模式的）。判宽不判紧，和虚拟屏那边
同一个取舍 —— 把真屏误判成占位屏，代价只是多开一次内屏；漏判的代价是黑屏。

### 🐛 救援不再看开关：还有第二条黑屏路径

原来整段「把内屏开回来」都写在 `guard 开关已打开` 后面。于是开关关着的人
（他没开过「有外接屏时自动关闭内置屏」）只要**在外接屏插着的时候手动关掉内屏**
（那会儿有外接屏可看，是允许的），再拔线 —— 规则只会回一句「开关没打开」，
然后就什么都不做了，同样是一块黑屏。

开关管的是「有外接屏时要不要顺手关内屏」这个**偏好**；而「没有任何真实屏在线」
是**故障**。现在救援排在开关判断之前，无论开关开没开都会执行。

### 🐛 内屏的 displayID 从「一个」改成「一串候选」

救援全靠「内屏的 displayID」。原来只记一个，而它一旦过期（系统重建过显示配置、
记录被清掉、重装过），同时刚好又没有外接屏 —— 那就真的一块屏都开不回来了。
现在按顺序试：关闭记录里的 → 历史见过的内屏 id（最多留 4 个）→ 名字缓存里像内屏的。
试不中的 id 不对应任何显示器，只会干脆地失败，代价远小于黑屏。

### ✨ 兜底巡检从 60 秒收到 10 秒

这个功能唯一的硬承诺就是「拔线必须亮屏」，而「配置变化」通知是有可能不来的。
一次巡检只是问一遍「现在该不该救」，代价远小于让用户对着黑屏等一分钟。
（顺带加了一道初筛：内屏还在在线列表里就直接返回，连模式列表都不枚举。）

### 🐛 合盖不是故障，别去惊动它

救援的判据是「没有真实的屏 + 内屏也不在线」，而**合盖之后 macOS 会把内屏
从显示配置里摘掉**（clamshell）—— 得到的状态和「内屏被本应用关掉了」一模一样。

不加区分地去开它有两个问题：合盖时内屏本来就不会亮（面板不亮），救援
**一点收益都没有**；而打开显示器之前会先声明一次用户活动把屏幕叫醒，
电脑装进包里的时候这样每 10 秒来一次，就是白白发热耗电。

所以现在合盖时按兵不动，开盖会触发一次显示配置变化，那时再救一点不迟
（通知万一丢了，还有下一次巡检兜着）。日志里也会带上「合盖」标记。

顺带确认了一件本来担心的事：**普通屏幕睡眠（不涉及合盖）时内屏仍在在线列表里**，
所以不存在「每 10 秒把刚睡着的屏幕点亮」这种问题 —— 实测过。

### ✨ 现场还原与诊断

- `--auto-test` 新增 `占位屏排除` 一行，以及 `屏幕睡眠 / 合盖` 状态；
  `--auto-test --fake-no-external` 可以把外接屏一律当成占位屏，
  在真机上复现「拔线后只剩随航残影」那一幕。
- 规则日志里每次配置变化都会记下被剔掉的虚拟屏和占位屏（`id「名字」`），
  以及当次是不是合盖。
- `--auto-scenarios` 从 19 条扩到 **38 条**，新增：占位屏识别、
  救援候选 id 的顺序与去重、开关关着也要救、合盖不惊动 / 开盖必救，
  以及一条防「亲手造黑屏」的回归（没有外接屏 + 内屏在线时，
  绝不能反过来把内屏关掉）。

### ℹ️ 一条已知边界

这套救援的前提是**应用在运行**。退出应用之后就没有人去救内屏了 ——
所以如果你把内屏关了又打算退出应用，先在菜单里把内屏开回来。
（要让它在任何情况下都兜住，需要把应用装成登录项，这一步还没做。）

## 1.4.0

卡片重排：右上角那三个点（「···」）去掉了，「开启」「HiDPI」两枚开关搬到那个位置；
**分辨率做成一条滑块，和亮度条严格对齐**；顺手修掉亮度滑块「左右都滑不到底」。

### ✨ 「···」去掉，开关搬到那个位置

那三个点又小又不好点，而且「点到哪算点到这三个点」本身就不清楚。
现在卡片右上角放的是**这张卡上最常拨的两枚开关** —— 上「开启」、下「HiDPI」，
标签在开关左边、右对齐，两枚开关严格排在同一条竖线上。

点卡片主体（缩略图 / 名称 / 型号那一块）仍然进详情页，悬停时整张卡的底色会提亮一点，
不再是「只有那三个点暗示这里能点」。详情页也顺势瘦身：**亮度、分辨率、开启、HiDPI
都在卡片上了，那一页就不再放第二遍**（同一个开关出现在两处，人会以为是两件事），
只留下卡片上放不下的：全部分辨率列表、重新检测 DDC、忘记这台显示器。

### ✨ 分辨率做成滑块，和亮度条对齐

一台屏一张卡，两条控制行：

```
☀ 亮度                    55%
━━━━━━━━●━━━━━━━━━━━━
⤢ 分辨率        HiDPI  1680 × 1050
━━━━━━●━━━━━━━━━━━━━
```

两条行的标签、轨道都从同一个 x 起、到同一个 x 止，轨道等宽。数值从「轨道右边那一小格」
挪到了**标签行的右端** —— 留在轨道右边的话，「1680 × 1050」比「55%」宽出一倍多，
两条轨道就会一长一短，反而对不齐。

滑块上的档位只收**和面板原生比例一致**的那一批（这台机器上：外接屏 13 档，内置屏 8 档），
每档在轨道上点一颗小圆点，拖动一档一档地跳。菜单里那份「常见档位」有三十多档，
做成滑块就是满屏小点、每格 4pt，根本拖不准 —— 那不是滑块该干的事。
「常见档位」以外的（例如给老游戏用的 640×480）走详情页的「全部分辨率」。

切一次模式屏幕会黑一下、窗口还要重排，所以**只在松手时真的切**：拖动中只改数字，
松手后菜单先收起来、等 0.25 秒再切、切完弹回来（和 HiDPI 同一条路，理由也一样，
见 1.3.0 那条「菜单收尾那一帧里改显示配置会被还原」）。拖回原来那一档则当作没动过，
不会为一次原地踏步把菜单收起来。

### 🐛 亮度滑块左右都滑不到底

拖到底了，滑块头离轨道两端还差 6.5pt —— 正好是圆头半径。原因是滑块头的位置比例是
从 `cell.knobRect` / `cell.barRect` 反推的，而系统把轨道按它自己的圆头尺寸往里缩过：
数值到 min/max 时 `knobRect` 落在 `barRect` 内缩 kd/2 的位置上，反推出来的 t 只能是
0.03…0.97，**永远到不了 0 和 1**。

比例 =（数值 − 下限）/ 区间，本来就是一个除法，现在自己算。截图逐像素核对过：
0% 时圆头左缘与轨道左端重合，100% 时右缘与右端重合。

### ✨ 顺带

- 卡片上那一行规格改成**面板的物理分辨率**（内置屏 `3360 × 2100 · 60 Hz`）。
  当前逻辑分辨率由分辨率滑块那一行说，这里再说一遍就是同一句话重复两遍，
  而只隔三行。物理分辨率是面板固定不变的属性，两者分工正好。
- 详情页横幅补上一行「当前 1680 × 1050 HiDPI · 面板 3360 × 2100 · 60 Hz」，
  并去掉与下方重复的亮度警告（下面那条禁用项里本来就有全文）。
- 关掉的屏存的那份快照多存了物理分辨率（旧记录读到 0 会退回逻辑分辨率）。
- 新增开发/诊断命令 `--modes`（打印分辨率滑块上的档位）、
  `--drag-res <卡片> <档位>`（走一遍「拖滑块并松手」的真实链路）、`--fake-bright <0-100>`。
  `--click-part` 的 `dots` 改为 `body`（那三个点没了）。

## 1.3.0

菜单换成**横排卡片**：几台显示器就并排几张卡，每张卡自带亮度、开启、HiDPI 三件套。
**被关掉的显示器不再从那一排里消失** —— 它照旧占一张卡，只是「开启」是关的，点一下就开回来。

### ✨ 卡片改成横排，关掉的屏也留在那一排里

1.2.0 的卡片虽然图形化了，但一列竖着摞下来，而且**关掉一台屏之后它会从这一排里消失**，
被挪到菜单底部单独列一行「（已关闭，点击重新打开）」。结果是：关掉内屏之后，
菜单里就只剩外接屏，看不见自己刚关了什么，想开回来还得去底部那一堆文字行里找。

现在按显示器并排成卡片，每张卡从上到下是：

| 元素 | 说明 |
| --- | --- |
| 缩略图 | 屏幕 + 支架/底座，**长宽比跟着真实分辨率走**（带鱼屏一眼认得出来），屏幕上还画了张壁纸 |
| 名称 + 徽章 | 「内置显示器 / 外接显示器 1 / 外接显示器 2」，主屏挂一枚「主显示器」徽章 |
| 型号 · 分辨率 | 系统给的型号名，下面一行是「2560 × 1440 · 60 Hz」 |
| 亮度 | 小太阳 + 「亮度」标签，一条细轨道滑块，右端是实时百分比 |
| 开启 | 这台屏的开关。**关掉的屏就在原地，拨回去就亮** |
| HiDPI | 开关 + 状态标签（HiDPI / 标准 / 不支持） |

卡片右上角的「···」（点卡片头部任意位置都行）进详情页，放卡片上摆不下的东西：
分辨率列表、关闭此显示器、重新检测 DDC、忘记这台显示器。

### ✨ 关掉的屏也有完整信息

以前那块被关掉的屏只剩一个名字，因为它一下线，分辨率、亮度、
HiDPI 状态就全都查不到了。现在**在关掉它的那一刻把这些抄一份存下来**，
所以关掉的屏照样能显示「1680 × 1050 · 60 Hz」和关闭前的亮度档位 —— 卡片不会留一片空白。

### 🐛 关掉显示器之后如果进程立刻退出，记录会丢

「已关闭」的记录关系到一个**已经黑掉的屏幕还能不能找回来**。而 `UserDefaults.set`
是把值交给 cfprefsd 异步落盘的，写入那一刻进程要是刚好没了（崩溃、强退、
命令行跑一次就退出），记录就丢了 —— 菜单里既没有它的卡片，也没法把它开回来。
现在写入后显式同步一次，不赌那个窗口。

### 🐛 菜单收尾那一帧里改显示配置会「成功但被还原」

切换 HiDPI / 分辨率这类显示配置更改，如果在菜单还在收尾的那一帧里发出去，
`CGDisplaySetDisplayMode` 会返回 success、配置也确实短暂变过，**然后被系统还原回去** ——
用户看到的就是「点了 HiDPI 没反应」，连个报错都没有。

现在分两手治：一是切显示配置一律**等菜单彻底收干净再动手**（延后 0.25 秒），
二是 `setMode` 改成**不相信返回值、只看观测结果**，没变就隔一拍再补一次
（和 `setEnabled` 同一套做法）。这条也顺带修好了「分辨率」子菜单里同样的隐患。

### ✨ 顺带

- 随航 / 隔空播放投出来的临时屏（iPad 一断开就永远回不来）不再赖在「已关闭」记录里，
  菜单不会挂着一张永远开不起来的卡片。
- 详情页新增「忘记这台显示器（从列表移除）」：显示器被关掉之后又拔了线，
  自动清理认不出它还会不会回来，这条给用户手动收尾。
- 新增命令行 `--display-on/--display-off <id>`、`--hidpi-toggle <id>` 用于诊断，
  以及 `--fake-off`、`--click-part`、`--hover-part` 三个开发参数。

## 1.2.0

菜单重新设计：从「一列文字」变成「一眼看得懂的显示器卡片」，顺手修掉亮度条偶尔变灰的问题。

### 🐛 亮度条的蓝色会丢

菜单里那条蓝得发亮的亮度条，有时候会**整条变成灰色** —— 同一份菜单里，外接屏那条灰了，
内屏那条还是蓝的，看截图的人只会以为「这台屏的亮度不可调」。

原因不在我们的数据，而在系统：**滑块的填充色只在 app 处于活跃状态时才画**。
菜单项里的自定义视图并不是每次都能等到活跃态，于是有的滑块拿到了颜色、有的没拿到。
现在滑块的轨道、填充、滑块头全部由我们自绘，颜色写死，跟活跃态彻底脱钩 —— 它不可能再变灰。

### ✨ 菜单改成了图形化的显示器卡片

- 一台屏一张卡：显示器图形 + 名称 + 分辨率，卡片下方就是这块屏自己的亮度条，
  前面一枚圆形图标点明这一行调的是亮度，轨道左端嵌着一枚小太阳标出「暗」这一头。
  哪条滑块管哪台屏，不用再靠对齐去猜。
- **点卡片进详情页**：HiDPI、分辨率、关闭此显示器、重新检测 DDC 都挪到这一页，
  顶部有「‹ 返回显示器列表」。详情页里也保留了那张卡片和亮度条，调分辨率时顺便就能改亮度。
- 全局开关「有外接屏时自动关闭内置屏」换成了一枚真正的开关，并补了一行小字说明它的行为。
- 显示器超过 3 台时卡片会分页，两侧出现翻页箭头和页码。
- 亮度数值平时收在卡片右上角，拖动滑块时才跳出来；写入失败会就地显示「无应答」。

### 🐛 菜单里的图标一度全是黑的

重做菜单时踩到的第二个坑，顺手一起修了：**卡片上的显示器图形、轨道里的小太阳、
返回箭头，全被画成了黑色**，深色菜单下就是一团黑疙瘩。

根因是 SF Symbol 的着色方式 —— 模板图走 `draw(in:from:operation:...)` 这条路径时
**不吃当前的填充色**，`配色.set()` 之后再画是无效的。修法是把颜色用
`SymbolConfiguration(paletteColors:)` 直接烘进图片里。

这个 bug 藏得比较深：黑色本身是「看得见」的，图形轮廓、位置、大小全对，
只有颜色不对，不盯着看很容易当成「设计就这样」放过去。

### 🐛 卡片、开关、返回一开始都点不动

改完之后最要命的一个坑：**所有自绘的行都点不动** —— 点卡片不进详情页、点开关不切换、
点返回不回去，整张菜单里只有亮度滑块还能拖。

根因是 AppKit 的一条规则：菜单项**挂了自定义视图之后，鼠标事件整个交给视图，
菜单不再代发 action**。于是「看着能点」的行全都成了摆设。滑块之所以没事，
是因为 NSSlider 自己处理鼠标，不走菜单动作那条路。

修法是让每个自绘行把点击自己转成菜单动作，统一走 `panelForwardClick`：
按下时先吃掉事件（不这么做菜单会在按下瞬间就关掉，轮不到抬起），抬起时再 `sendAction`。

顺带一个发现：**`screencapture` 会让菜单结束跟踪**。所以自测里「截一张图再点下一步」
必然失败，而且表现是「菜单关掉了、什么都没发生」—— 跟「这行本来就不能点」长得一模一样。
现在自测改成整条链路一次跑完，只在最后截一次图。

### 🐛 「已关闭」那两行把菜单撑宽了

外接屏接上、内屏被规则关掉时，列表里会多出「XX（已关闭，点击重新打开）」。
这句话太长，会把整张菜单从 372pt 撑到 420pt，右边空出一大块，卡片看着就像没对齐。

现在这两行也改成自绘，宽度锁死在面板宽度上；名字再长也只是中间省略，
右端留一个「已关闭 · 点此重开」的提示。想精确点它的时候，
新增的 `--hits` 会把每一行的屏幕坐标直接打出来。

## 1.1.1

修掉 1.1.0 里「拔掉外接屏，内屏却没有亮回来」的问题。根因不在判断逻辑，而在**认错了屏幕**。

### 🐛 拔掉外接屏后内屏不亮

macOS 26 在**所有真实显示器都不可用时**，会自己造一块虚拟显示器维持显示输出。
它的 `CGDisplayIsBuiltin` 返回 0，于是被当成了「外接屏还接着」，规则判定「内屏本来就没开」，
什么都不做 —— 而用户面前只有一块他根本看不见的虚拟屏，等于黑屏。

现在会把它认出来并排除（判据是它的 EDID 是 `'unkn'` / `'virt'` 两个四字符码），
从菜单和所有判断里都剔掉；它也不会再参与「关掉最后一台显示器」的保护计数。

顺带修掉一个相关的老问题：**在只有虚拟屏时，`--selftest` 和 `--dump-menu` 会永久卡死**。
原因是它们会对虚拟屏做 DDC 探测，而虚拟屏不会应答 I²C。把它排除后这两个诊断恢复正常。

### 🐛 外接屏被误记成「内置屏」

关闭一台显示器之后再去查 `CGDisplayIsBuiltin`，拿到的是垃圾值（实测外接屏会被判成内置屏）。
而「哪条记录是内屏」正是拔线后把谁开回来的唯一依据，标错就等于没法恢复。
改成在关闭**之前**查，和 EDID 一起记下来。

### 🛡 就算判断错了，也要能救回来

「拔掉外接屏就把内屏开回来」一旦失效，用户面对的是黑屏，而且**没有任何办法自救**
（打不开菜单，也看不到界面）。所以这一版给它加了三层保险：

- **打开内屏不再是一次性尝试。** 失败会退避重试 6 次（0.5s → 15s）。原来的实现只试一次，
  失败就把状态记成「已处理」，之后再也不会尝试。
- **多一层低频巡检。** 每分钟检查一次，只有真的处于「没有外接屏、内屏却不在线」时才动手。
  这是为了防止配置变化通知在某些情况下没送到。
- **记住内屏是谁。** 关闭记录有可能丢了（用户手动开回过、系统重建过配置），
  记录一没规则就会以为「不是我关的，不关我事」。现在额外记一份内屏的 displayID 兜底。

另外，打开内屏这条路径**不受「屏幕睡眠就不动手」的限制**：宁可多亮一块屏，
也不能让用户对着一块黑屏。

### 🔎 新增：规则运行日志

这个功能失效的样子是黑屏，而黑屏状态下用户没法打开菜单排查，所以每次判断都落一份日志。

```
"$APP" --auto-log          # 打印最近 80 条
```

文件在 `~/Library/Application Support/Display Master/auto-rule.log`，
记着「收到配置变化时的在线列表 / 判定结论 / 执行结果 / 重试」。
反馈问题时把它贴出来，一眼就能看出卡在哪一步。

### 🧪 新增：命令行诊断入口

```
"$APP" --auto-test         # 只读诊断：当前状态 + 这一轮准备做什么、为什么
"$APP" --auto-test --apply  # 真的执行一次
"$APP" --auto-scenarios    # 18 条构造场景全跑一遍，不接触真实显示器
"$APP" --set-display <id> on|off [--force]   # 直接开关指定显示器（调试用）
```

`--set-display` 存在的理由很实际：这个功能最要紧的场景是「拔掉外接屏」，
而真机上没法为了测试反复拔线。用它把当前唯一在线的屏关掉，就能复现那一瞬间。
`--force` 会绕过「不许关掉最后一台」的保护，只在确认能把屏幕开回来时用。

## 1.1.0

加了一个新开关，顺手把「哪条记录是笔记本内屏」这件事记准了。

### 🆕 有外接屏时自动关闭内置屏

菜单里多了一项开关。打开之后，接上外接显示器就关掉笔记本内屏，拔掉再自动开回来。

有两处容易做错，实现里都避开了：

- **不去抢用户的手动操作。** 只在外接屏「接上」和「拔掉」这两个瞬间动手。
  你临时把内屏开回来干点活，它不会立刻又给你关掉。
- **屏幕睡眠时不动显示配置。** 睡眠状态下改配置系统会拒绝，还会把屏幕平白唤醒。
  碰到这种情况就什么都不做，等下一次配置变化再说。

拔掉外接屏之后必须保证内屏是亮的 —— 这条一旦判断错，用户面对的就是一块黑屏。
所以判定逻辑拆成了不依赖硬件的纯函数，`--auto-scenarios` 会把八种情形全跑一遍
（包括「拔屏时内屏被关着」这条），每条都打印判断理由。真机上没法随便插拔线，
这样至少能把分支钉住。

### 🐛 修复：「已关闭」记录分不出内屏和外接屏

1.1.0 之前的记录只存了名字和 EDID，没记这块屏是不是笔记本内屏。新的自动开关
要认出内屏才能把它开回来，所以记录里补了 `isBuiltin` 字段。旧记录会按名字推断
一次（Apple 给内屏起的名都带 "Built-in"），升级后不需要重新设置什么。

### 🔧 命令行诊断

- `--auto-test` —— 打印开关状态、在线显示器、当前这一轮会做什么判断。
  加 `--apply` 才真的执行，加 `--restore` 会把测试关掉的内屏开回来。
- `--auto-scenarios` —— 用构造出来的场景跑判定逻辑，不接触真实显示器。

### 顺带

`build.sh` 现在会在 Xcode 许可证还没同意、`swift` 拒绝工作的时候，自动改用
CommandLineTools 工具链把构建跑完。重装或者更新过 Xcode 之后很容易撞上这个，
提示里也写了怎么切回 Xcode 工具链。

## 1.0.1

修复两个和「显示器睡眠」直接相关的 bug。

### 修复：显示器睡眠唤醒后，亮度滑块失灵

现象是滑块还在、也能拖，但显示器亮度纹丝不动；以前只能靠手动点「重新检测 DDC」
或者给显示器断电才能救回来。原因是唤醒后 DDC/CI 的 I²C 通道会「哑掉」
（句柄还在、也不报错，但读不出也写不进），而旧版本有几处会让这个状态**卡死**：

- 重新枚举 DDC 服务时如果这一次一个都没枚举到（唤醒瞬间很容易发生），
  旧版本会把已有句柄列表整个清空 —— 通道就此永久失联，直到用户手动重检。
  现在改为**保留上一次的句柄**，并支持按需重扫。
- 读失败后进入的 20 秒冷却会把写入一起挡住，这段时间里怎么拖都没反应。
  现在读写失败都会**自动重建一次句柄并重试**，重建相当于热插拔时系统自己做的事，
  不需要给显示器断电（按 10 秒限流，避免拖动时反复重建）。
- 读亮度的节流在缓存为空时会直接返回 nil，导致滑块「消失」。现在缓存为空时不再走节流。

同时新增：监听系统唤醒与屏幕配置变化通知，唤醒后自动重建 DDC 通道，
用户不需要知道「重新检测 DDC」这个按钮的存在 —— 它现在只是手动兜底。

### 修复：打开一台已关闭的显示器后，菜单里仍显示「点击重新打开」

实测确认了两个叠加的原因：

- `CGCompleteDisplayConfiguration` **在屏幕睡眠时会返回 1014 而不是 0**，
  但显示配置其实已经生效。旧版本只看这个返回值，于是「已关闭」记录不删，
  菜单里就一直挂着那行入口。现在改为**只看观测结果**：动作发出去后轮询在线列表，
  以「显示器真的回来了」为准，不再相信 API 返回值。
- 显示器重新上线时系统可能分配**新的 displayID**，只按 id 记账的记录永远清不掉。
  现在「已关闭」记录会连 EDID 三要素（厂商/型号/序列号）一起存下来，按硬件身份认领。
- 顺带修掉一个会让菜单**彻底停止刷新**的问题：旧版本用布尔量标记「正在拖滑块」，
  一旦某次收不到 `mouseUp`，菜单就再也不重建了 —— 屏幕状态怎么变，菜单都是旧的。
  现在改成带时间戳的状态（会自己过期），并在菜单关闭时复位。
- 另外：`NSScreen.screens` 在一台显示器被重新打开之后**不会更新**（实测 3 秒仍滞后），
  而 CoreGraphics 的在线列表 0.2 秒内就恢复了。显示器列表因此改为以 CoreGraphics 为准，
  名字优先取 NSScreen、取不到时用缓存兜底。

### 打包：新增 DMG 安装包

从 1.0.1 起，Release 里除了 `.zip` 还提供 `.dmg`：

- 双击挂载后是一个带背景图、卷图标和「拖到这里安装」窗口的磁盘映像，
  把左边的 App 拖到右边的「应用程序」就装完了，不用先解压。
- DMG 里的 App 带着完整的代码签名封装，也就不会遇到
  「解压工具弄丢元数据、系统报应用已损坏」这类问题。
- 窗口布局（尺寸、图标位置、背景图）由 `Tools/make-dsstore.py` 直接写出 `.DS_Store`，
  **不依赖 Finder 自动化权限**，所以别人 clone 下来也能直接打出一样的 DMG。
- 新增 `./build.sh --dmg` 一条命令完成「构建 + 安装 + 打包 DMG」。

### 其他

- 屏幕睡眠时改显示配置会被系统拒绝，现在会先**唤醒屏幕再改**（屏幕上出现菜单，
  说明人就在机器前，唤醒是符合预期的行为）。
- 亮度写入失败时，滑块那一行会就地显示「无应答」，而不是让人对着没反应的滑块反复拖。
- 新增三个回归测试：`--toggle-test`、`--wake-test`、`--ddc-recover-test`。

## 1.0.0

首个版本。

- 单独开关（断开/恢复）某台已连接的显示器，绝不关闭最后一台
- 亮度调节：内置屏走 `DisplayServices`，外接屏走 DDC/CI（`IOAVService`）
- HiDPI 开关：优先在同一逻辑分辨率上换渲染倍率，没有对应变体时切最接近的档位
- 分辨率切换：默认只列常见档位，可展开完整列表（5120×2880 面板实测 220 项）
- 亮度滑块直接放在一级菜单
- 命令行诊断：`--selftest` / `--dump-menu` / `--hidpi-test` / `--ddc-test` / `--ddc-storm`
